var replaceDots = function(str) {
  return str.replace(/\./g, '-')
}

/**
Special karakter dalam regex harus di beri backslash salah satunya adalah titik.
function replace() mengganti dengan input yang diinginkan
**/
